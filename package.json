{
  "name": "@habx/lib-client-intl",
  "version": "0.3.0",
  "description": "Lib Client Intl",
  "main": "./dist/index.js",
  "types": "./dist/typings/index.d.ts",
  "scripts": {
    "clean": "rimraf ./dist",
    "build": "npm run clean && npm run build:types && bili",
    "build:watch": "npm run clean && npm run build:types && bili --watch",
    "build:types": "tsc --emitDeclarationOnly",
    "postbuild": "node ./scripts/postbuild.js",
    "publish": "cd dist && npm publish && cd ..",
    "lint": "eslint \"src/**\"",
    "test": "echo TODO"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/habx/lib-client-intl.git"
  },
  "author": "HABX Front End Team",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/habx/lib-client-intl/issues"
  },
  "homepage": "https://github.com/habx/lib-client-intl#readme",
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{ts,tsx}": [
      "eslint --fix"
    ]
  },
  "dependencies": {
    "lodash": "^4.17.15",
    "react": "^16.12.0",
    "react-intl": "^4.0.0"
  },
  "devDependencies": {
    "@habx/eslint-config-client": "^2.6.1",
    "@types/lodash": "^4.14.149",
    "@types/react": "^16.9.19",
    "bili": "^4.7.4",
    "husky": "^4.0.0",
    "lint-staged": "^10.0.1",
    "rimraf": "^3.0.0",
    "rollup-plugin-typescript2": "^0.26.0",
    "typescript": "^3.6.4"
  }
}
